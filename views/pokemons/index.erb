<h2>All Pokemons</h2>

<% @pokemons.each do |pokemon| %>
  <div class='pokemon'>
    <h3><a href='/pokemons/<%= pokemon.id %>'><%= pokemon.name %></a></h3>
    <img src="<%= pokemon.img_url%>">
  </div>
<% end %>
